# AISocialGame 后续开发里程碑

> 更新时间：2026-05-17

## 1. 总目标

AISocialGame 的目标是做一个真人玩家与 AI 共同参与的社交游戏平台。平台需要能够持续接入多种社交游戏，并让不同游戏中的 AI 角色根据玩法、身份、局势、人设和历史互动做出合适行动。AI 不追求冒充真人，而是在明示 AI 身份的前提下，表现出更自然的语言、更稳定的人格、更合理的推理与策略，以及接近真人的局内思考感。

## 2. 当前基础

项目当前已经具备以下能力：

- 已有玩法：狼人杀、谁是卧底；海龟汤在前端配置中处于 `coming_soon`。
- 房间与实时能力：创建房间、入座、AI 补位、WebSocket 状态推送、连接状态展示、断线托管。
- AI 决策能力：`AiDecisionService` 已支持基于局面上下文生成发言、投票与狼人杀夜晚行动，并有规则兜底。
- 社交留存雏形：快速匹配、好友、成就、回放、观战、新手引导已有前端入口或本地降级。
- 外部能力：已接入 user-service、pay-service、ai-service，具备 SSO、积分、AI Chat/OCR/Embeddings 代理等基础。

当前主要短板：

- AI 缺少长期记忆、局内信念状态、反思总结、质量评测和可运营调参闭环。
- 游戏流程仍集中在 `GamePlayService`，新增游戏会继续增加分支复杂度。
- 回放、好友、匹配等部分能力前端可用但后端化不足，难以支撑长期运营和 AI 质量分析。
- 玩法内容、Persona、Prompt、模型策略和 AI 行为质量缺少管理后台与审计能力。

## 3. 产品原则

- AI 身份默认明示：前端继续展示 AI 标记，不让 AI 冒充真人。
- 近期优先级为 AI 质量：先让现有狼人杀/谁是卧底中的 AI 更会玩、更像一个稳定角色，再扩大玩法数量。
- 首个新增游戏为海龟汤：它与 LLM 的语言推理、主持、追问、线索控制能力匹配度最高，也已在当前前端游戏配置中预留。
- 新能力优先沉淀为平台能力：新增玩法不应只堆单个游戏页面，而要推动 GameEngine、事件流、AI Adapter、评测与运营后台逐步成型。

## 4. 里程碑总览

| 里程碑 | 优先级 | 主题 | 核心产出 |
|---|---|---|---|
| M1 | P0 | AI 拟人质量闭环 | 记忆、信念、反思、难度、质量评测；开发记录见 `doc/milestones/m1-ai-quality-loop/development.md` |
| M2 | P0 | 结构化事件与回放/质检底座 | 服务端事件流、回放、AI 复盘数据；开发记录见 `doc/milestones/m2-structured-replay/development.md` |
| M3 | P0 | GameEngine 插件化架构 | 游戏引擎抽象、统一 action、现有玩法迁移；开发记录见 `doc/milestones/m3-game-engine-plugin-architecture/development.md` |
| M4 | P1 | 新增海龟汤玩法 | AI 主持、题库、提问与解谜流程 |
| M5 | P1 | 社交平台化与留存 | 后端化好友/匹配/观战/战报/成长体系 |
| M6 | P1 | AI 运营后台与安全治理 | Persona/Prompt/模型/成本/审计/灰度 |

已完成里程碑的分目录归档入口见 `doc/milestones/README.md`。

## 5. M1：AI 拟人质量闭环

### 目标

让狼人杀和谁是卧底中的 AI 从“能行动”升级为“会根据局势、人设和历史互动做出稳定、有依据、少重复的行动”。

### 交付项

- AI 局内信念状态：
  - 为每个 AI 玩家维护对其他玩家的怀疑度、可信度、已知线索、关键发言引用。
  - 狼人杀需区分公开信息、阵营信息、个人身份技能信息，避免泄露不可见信息。
  - 谁是卧底需记录每轮描述相似度、异常表达、跟随/反向跟随行为。
- AI 短期记忆与跨局 Persona 记忆：
  - 局内记忆用于当前对局推理。
  - Persona 记忆用于稳定口吻、偏好和常见策略，不直接保存敏感用户隐私。
- AI 反思总结：
  - 每轮或每局结束后生成结构化总结：本轮判断、错误判断、关键证据、下一步计划。
  - 总结进入后续 Prompt 上下文，但必须限制长度和可见信息范围。
- 难度与风格分层：
  - `difficultyLevel` 从简单/娱乐/进阶扩展为可评测策略等级。
  - Persona 的 `speechStyle` 与 `strategyStyle` 要在发言、投票、夜晚行动中持续体现。
- AI 质量评测：
  - 对每次 AI 决策记录：输入上下文摘要、输出、规则校验结果、兜底标记、耗时、模型、原因。
  - 增加离线评测脚本或测试夹具，覆盖非法泄密、非法目标、重复模板、无依据投票、角色策略不一致。

### 验收标准

- 谁是卧底 1 真人 + AI、3 真人 + AI 对局可完整结束，AI 描述不会直接暴露词语，投票能引用局内线索。
- 狼人杀 1 真人 + AI、3 真人 + AI 对局可完整结束，狼人、预言家、女巫的 AI 行动均合法且不泄露不可见信息。
- AI 决策日志中能看到信念状态、关键依据、是否兜底、模型调用耗时。
- 同一 Persona 连续多轮发言风格稳定，重复模板化发言明显减少。

### M1 实现记录（2026-05-16）

- 新增服务端 AI 质量闭环：信念快照、局内短期记忆、Persona 跨局记忆、决策 trace、质量 flags 与安全日志 metadata。
- 新增管理端 API：AI 决策 trace 查询、Persona 记忆查询与重置。
- 玩家端继续只展示 AI 身份、发言和公开行动，不展示隐藏身份、隐藏词语、完整 Prompt 或内部推理链。
- 详细架构见 `doc/modules/ai-quality-loop-module.md`。
- 分目录开发记录见 `doc/milestones/m1-ai-quality-loop/development.md`。

### 依赖

- 现有 `AiDecisionService`、`GameLogEntry` 结构化字段、`Persona` 扩展字段。
- M2 的结构化事件流完成后，可进一步提升 M1 的评测与复盘质量。

## 6. M2：结构化事件与回放/质检底座

### 目标

把当前对局文本日志升级为结构化事件流，为回放、观战、AI 质量评估、赛后战报、争议审计和未来训练数据沉淀提供统一底座。

### 交付项

- 新增结构化事件模型：
  - 事件包含房间、游戏、序号、阶段、轮次、行为人、目标、公开/私有/上帝视角可见性、事件数据和时间。
  - 覆盖开局、身份/词语分配、发言、投票、夜晚行动、阶段切换、淘汰、结算、断线托管、AI 决策摘要。
- 新增对局存档：
  - 保存玩家列表、最终身份、胜负、轮次、时长、关键事件摘要、AI 质量指标。
- 服务端回放 API：
  - 支持按游戏、玩家、时间分页查询历史对局。
  - 支持上帝视角、玩家视角、公开视角的事件过滤。
- 前端回放升级：
  - 将当前本地回放降级逐步切换为服务端回放数据。
  - 回放页面支持阶段轴、发言/投票流、身份揭示、AI 决策原因展示。

### 验收标准

- 任意完整狼人杀/谁是卧底对局结束后都生成服务端存档。
- 回放可按事件顺序稳定播放，私有身份和夜晚行动不会在玩家视角提前泄露。
- AI 质检可从结构化事件中定位某次发言/投票的上下文和结果。

### 依赖

- 现有 `GameState.logs` 和前端回放页面。
- 为 M1 的质量评测、M5 的社区战报、M6 的后台审计提供基础数据。

### M2 实现记录（2026-05-16）

- 新增服务端结构化事件流：`game_events` 记录事件序号、阶段、轮次、行为人、目标、可见性和结构化数据。
- 新增服务端单局归档：`game_archives` 记录房间、玩法、胜负、玩家快照、事件数和 AI trace 摘要。
- 新增 `ReplayController`：提供回放列表、详情和 `PUBLIC` / `PLAYER` / `GOD` 视角事件查询。
- 前端 `/replays` 与 `/replay/:archiveId` 优先读取服务端回放，服务端不可用时保留原本地存档降级。
- 详细架构见 `doc/modules/replay-event-module.md`。
- 分目录开发记录见 `doc/milestones/m2-structured-replay/development.md`。

## 7. M3：GameEngine 插件化架构

### 目标

将当前集中式游戏流程拆成可注册的 GameEngine，让新增游戏不再依赖在 `GamePlayService` 中堆叠大量 `gameId` 分支。

### 交付项

- 定义统一游戏引擎接口：
  - 生命周期：开局、行动处理、自动推进、结算。
  - 视图过滤：按玩家视角返回可见状态。
  - 玩法元数据：阶段、角色、最小/最大人数、配置项、前端渲染提示。
- 定义 AI Adapter：
  - 每个游戏声明 AI 可执行动作、可见上下文、Prompt 模板、合法性校验和兜底策略。
  - `AiDecisionService` 从具体游戏分支转向读取游戏 Adapter。
- 迁移现有玩法：
  - 先迁移谁是卧底，再迁移狼人杀。
  - 保持现有 API 路径兼容，避免前端大规模同步改动。
- 新增游戏注册机制：
  - 后端 `GameRepository` 与前端 `games.ts` 最终应由同一份服务端元数据驱动，减少重复配置。

### 验收标准

- 谁是卧底/狼人杀迁移后，原有单元测试和真实对局验收不回退。
- 新增一个玩法时，只需要新增引擎、AI Adapter、玩法配置和必要页面，不需要改主流程分支。
- `GamePlayService` 主要负责鉴权、房间解析、持久化、推送，不再承载具体玩法规则。

### 依赖

- M1 的 AI 决策抽象经验。
- M2 的统一事件记录。

### M3 实现记录（2026-05-17）

- 新增 `GameEngine`、`GameEngineRegistry`、阶段/角色定义和开局校验结果类型。
- 新增 `UndercoverGameEngine` 与 `WerewolfGameEngine`，现有玩法由 engine 注册表承接入口。
- `GamePlayService` 改为编排与兼容层，通过 registry 分发到具体 engine。
- 新增统一 `/api/games/{gameId}/rooms/{roomId}/action` 接口，旧 `/speak`、`/vote`、`/night-action` 保持兼容。
- 前端新增 `useGameEngine` 和玩法组件 registry，现有房间页动作提交切换到统一 action。
- 详细架构见 `doc/modules/game-engine-module.md`。
- 分目录开发记录见 `doc/milestones/m3-game-engine-plugin-architecture/development.md`。

## 8. M4：新增海龟汤玩法

### 目标

将海龟汤作为首个新增游戏落地，验证平台能支持合作式语言推理玩法，并验证 AI 可担任主持人与玩家两类角色。

### 交付项

- 海龟汤基础流程：
  - 房主选择题目或随机题目。
  - AI 主持人持有汤面、汤底、关键线索和禁答规则。
  - 玩家轮流提问，主持只能回答“是 / 否 / 不重要 / 无法回答 / 接近但不准确”。
  - 玩家可提交最终解答，主持判断是否通过。
- 题库与内容安全：
  - 题目结构包含汤面、汤底、核心线索、误导点、难度、标签。
  - 主持 Prompt 必须防止提前泄底，不得主动说出汤底关键答案。
- AI 玩家策略：
  - AI 玩家能根据已知问答提出新的有效问题，避免重复提问。
  - 支持娱乐型、逻辑型、脑洞型 Persona。
- 前端房间页面：
  - 展示汤面、问答历史、已确认线索、最终解答输入、主持判定。

### 验收标准

- 1 真人 + AI 玩家 + AI 主持可完整解出一局。
- 多真人 + AI 玩家可共同提问，AI 提问不会大量重复或直接作弊。
- 主持在常见诱导问题下不提前泄露汤底。

### 依赖

- M3 完成后实现成本最低；如果业务需要提前上线，可先以轻量独立引擎实现，再在 M3 中迁移。

## 9. M5：社交平台化与留存

### 目标

把当前“能玩一局”升级为“玩家愿意反复回来玩”，并让 AI 成为提升氛围、补齐人数、降低新手门槛的核心能力。

### 交付项

- 后端化社交能力：
  - 好友、邀请、最近同玩、黑名单、房间内快捷加好友。
  - 快速匹配支持按游戏、人数、难度、是否允许 AI、是否新手友好筛选。
- 后端化成长与内容：
  - 成就、排行榜、玩家统计、常玩 Persona、最佳 AI 队友/对手。
  - 对局战报和社区分享，支持从 M2 回放生成摘要。
- AI 社交辅助：
  - 房间暖场、规则讲解、新手提示、赛后点评。
  - 缺人时智能补位，匹配到真人后可让 AI 退场或转观战助手。

### 验收标准

- 新用户可通过快速匹配进入一局适合自己的真人+AI 房间。
- 赛后能看到战报、关键回合、个人表现和下一局推荐。
- 好友邀请、观战、回放分享形成完整闭环。

### 依赖

- M2 的回放/战报数据。
- M3 的游戏元数据。

## 10. M6：AI 运营后台与安全治理

### 目标

让 AI 行为可运营、可审计、可灰度、可控成本，避免只依赖代码发布调整 Prompt 和 Persona。

### 交付项

- Persona 管理：
  - 管理人设、头像、语气、策略、难度、可用游戏、记忆种子和禁用状态。
- Prompt 与模型策略：
  - 按游戏/动作/难度/实验组管理 Prompt。
  - 支持模型路由、超时、重试、成本预算和降级策略。
- AI 质量看板：
  - 展示模型调用量、成功率、兜底率、平均耗时、单局成本、玩家反馈、异常输出。
- 安全治理：
  - 明示 AI 身份作为默认规则。
  - 敏感内容过滤、不可见信息泄露检测、恶意 Prompt 注入检测。
  - 支持运营封禁某个 Persona、某类 Prompt 或某个模型策略。

### 验收标准

- 运营无需改代码即可调整指定玩法下某个 Persona 的风格和难度。
- 异常 AI 输出能定位到模型、Prompt、上下文、房间、玩家和事件。
- AI 成本和质量指标能按天、玩法、模型、Persona 汇总。

### 依赖

- M1 的决策日志。
- M2 的事件流。
- 管理后台现有 admin 模块。

## 11. 参考资料与设计启发

- [Generative Agents: Interactive Simulacra of Human Behavior](https://arxiv.org/abs/2304.03442)：可信拟人行为需要观察、记忆、反思和计划，对 M1 的记忆/反思设计有直接参考价值。
- [Werewolf Arena: A Case Study in LLM Evaluation via Social Deduction](https://arxiv.org/abs/2407.13943)：将狼人杀作为 LLM 社交推理评测场景，强调欺骗、推理、说服和动态发言机制，对 M1/M2 的 AI 评测有参考价值。
- [AIWolf Project](https://aiwolf.org/en/)：长期围绕狼人杀智能体竞赛建设工具、规则和评测环境，说明社交推理玩法适合作为 AI 游戏平台的核心基准。
- [WOLF: Werewolf-based Observations for LLM Deception and Falsehoods](https://arxiv.org/abs/2512.09187)：用狼人杀拆分评估欺骗生成、欺骗识别、信任变化等能力，对后续社交推理质量指标设计有参考价值。
- [Voyager: An Open-Ended Embodied Agent with Large Language Models](https://arxiv.org/abs/2305.16291)：技能库、自验证和持续学习思路可作为远期 AI 游戏技能库参考，但不作为 M1-M2 的前置依赖。

## 12. 推进规则

- 每个里程碑开始前，先确认该里程碑是否仍是当前最高优先级。
- 每个里程碑交付必须包含：代码实现、测试/验收记录、相关 `doc` 更新，并在 `doc/milestones/<milestone-id>/development.md` 记录阶段修改。
- 涉及 AI 行为的功能必须同时考虑：可见信息边界、非法动作兜底、成本控制、日志审计。
- 涉及新游戏的功能必须优先沉淀通用能力，避免只为单个玩法写死流程。
